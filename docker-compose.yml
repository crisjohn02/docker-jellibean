services:
  php82:
    build:
      context: ./apps/ide
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "9001:9001"
    networks:
      - jb-network

networks:
  jb-network:
    driver: bridge